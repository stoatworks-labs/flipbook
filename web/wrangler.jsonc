// Flipbook web demo — static-assets-only Worker.
// Deploy from web/: cf-run npx wrangler deploy  (see reference_cloudflare_access)
{
  "name": "flipbook",
  "compatibility_date": "2026-08-04",
  "assets": {
    "directory": "./public"
  },
  "routes": [
    { "pattern": "flipbook.stoatworks-labs.com", "custom_domain": true }
  ]
}
